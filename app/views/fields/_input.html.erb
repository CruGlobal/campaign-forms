<div class="form-group">
  <label for="<%= "cf_#{field.name}" %>"><%= field.label_value %></label>
  <input id="<%= "cf_#{field.name}" %>" class="form-control" type="<%= field.input %>" name="<%= field.name %>" placeholder="<%= field.placeholder_value %>" <%== 'required' if field.required %>>
</div>
